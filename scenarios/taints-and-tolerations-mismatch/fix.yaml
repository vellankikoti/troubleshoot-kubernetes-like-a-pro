apiVersion: v1
kind: Pod
metadata:
  name: fixed-taints-and-tolerations-pod
spec:
  containers:
  - name: nginx
    image: nginx:latest
  tolerations:
  - key: "key"
    operator: "Equal"
    value: "value"
    effect: "NoSchedule"
