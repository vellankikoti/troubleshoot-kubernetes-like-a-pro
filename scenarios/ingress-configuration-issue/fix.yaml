apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fixed-ingress
spec:
  rules:
  - host: myapp.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: my-service
            port:
              number: 80
  ingressClassName: nginx
