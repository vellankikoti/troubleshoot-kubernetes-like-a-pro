apiVersion: v1
kind: Pod
metadata:
  name: fixed-oom-killed-pod
spec:
  containers:
  - name: nginx
    image: nginx:latest
    resources:
      requests:
        memory: "1Gi"
      limits:
        memory: "2Gi"
