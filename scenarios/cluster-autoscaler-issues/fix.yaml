apiVersion: v1
kind: Pod
metadata:
  name: fixed-cluster-autoscaler-pod
spec:
  containers:
  - name: nginx
    image: nginx:latest
    resources:
      requests:
        memory: "1Gi"
        cpu: "500m"
      limits:
        memory: "2Gi"
        cpu: "1"
